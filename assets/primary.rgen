#version 460
#extension GL_EXT_ray_tracing : require

layout(set = 0, binding = 0) uniform writeonly image2D u_imgOutput;

void main() {
    vec3 outputColor = vec3(0, 0, 1);
    imageStore(u_imgOutput, ivec2(gl_LaunchIDEXT.xy), vec4(outputColor, 1.0));
}
